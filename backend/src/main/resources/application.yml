spring:
  application:
    name: devoluciones


  datasource:
    url: jdbc:postgresql://localhost:5432/devoluciones
    username: devoluciones
    password: Devoluciones2026Local
    driver-class-name: org.postgresql.Driver


  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: false


  flyway:
    enabled: true
    locations: classpath:db/migration
